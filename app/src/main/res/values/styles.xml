<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat">
        <!-- Customize your theme here. -->
    </style>

    <style name="StartTaskToggleButtonTheme">
        <item name="android:textOn">@string/common_start_task_btn_toggle_off</item>
        <item name="android:textOff">@string/common_start_task_btn_toggle_on</item>
        <item name="android:textColor">@color/common_toggle_button</item>
        <item name="android:textSize">22sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:padding">5dp</item>
    </style>
</resources>
